# ── YouTube API (original Python project) ────────────────────────────────────
YOUTUBE_API_KEY=your_api_key_here
YOUTUBE_LIVE_CHAT_ID=your_live_chat_id_here
# optionally you can provide the video ID instead of the live chat ID
# YOUTUBE_VIDEO_ID=your_video_id_here
# the location where the live chat ID will be cached after looking it up
# CHAT_ID_CACHE_FILE=chat_id.cache

# ── Docker chat scraper ────────────────────────────────────────────────────────

# Your YouTube channel URL.
# The scraper navigates to this URL + /live to find the active livestream.
YOUTUBE_CHANNEL_URL=https://www.youtube.com/@YourChannelName

# Password required to access Firefox via noVNC.  Set a strong value.
VNC_PASSWORD=your-secure-password-here

# Cloudflare Tunnel token for secure remote access from outside your home.
# Get a free token at: https://dash.cloudflare.com → Zero Trust → Tunnels
# Remove or leave blank if you only need local network access.
CLOUDFLARE_TUNNEL_TOKEN=your-cloudflare-tunnel-token-here

# Seconds to wait between DOM polls (lower = more real-time, higher = less CPU)
POLL_INTERVAL=2

# Seconds to wait before retrying if no livestream is currently active
RETRY_INTERVAL=60
